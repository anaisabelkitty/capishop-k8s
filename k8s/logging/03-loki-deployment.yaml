# Deployment de Loki, donde se juntan y guardan todos los logs del cluster
# Monta su config del ConfigMap y los logs en el PVC
apiVersion: apps/v1
kind: Deployment
metadata:
  name: loki
  namespace: monitoring
  labels:
    app: loki
spec:
  replicas: 1
  selector:
    matchLabels:
      app: loki
  template:
    metadata:
      labels:
        app: loki
    spec:
      # Corre como root para poder escribir en el volumen
      securityContext:
        runAsUser: 0
        fsGroup: 0
      containers:
        - name: loki
          image: grafana/loki:2.9.4
          # Le indica el archivo de configuración a usar
          args:
            - -config.file=/etc/loki/loki.yaml
          ports:
            - containerPort: 3100
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "300m"
          # Monta la config y el disco donde guarda los logs
          volumeMounts:
            - name: config
              mountPath: /etc/loki
            - name: storage
              mountPath: /loki
      # config viene del ConfigMap y storage del PVC
      volumes:
        - name: config
          configMap:
            name: loki-config
        - name: storage
          persistentVolumeClaim:
            claimName: loki-pvc
