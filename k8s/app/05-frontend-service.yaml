apiVersion: v1
kind: Service
metadata:
  name: capishop-frontend
  namespace: default
  labels:
    app: capishop-frontend
spec:
  type: NodePort
  selector:
    app: capishop-frontend
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30082
