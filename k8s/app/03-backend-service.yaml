apiVersion: v1
kind: Service
metadata:
  name: capishop-backend
  namespace: default
  labels:
    app: capishop-backend
spec:
  type: ClusterIP
  selector:
    app: capishop-backend
  ports:
    - port: 3000
      targetPort: 3000
